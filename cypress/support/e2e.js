// Cypress support file - se pueden añadir comandos personalizados aquí
// import './commands'
// Para hacer pruebas End to End (E2E)

// Ejemplo de comando personalizado para iniciar sesión
/*Cypress.Commands.add('login', (email, password) => {
  cy.visit('/login');
  cy.get('input[name="email"]').type(email);
  cy.get('input[name="password"]').type(password);
  cy.get('button[type="submit"]').click();
});*/